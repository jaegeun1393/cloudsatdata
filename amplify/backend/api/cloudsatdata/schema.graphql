type Studentlst @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  sid: Text
  tid: Text
}

type Usersat @model @auth(rules: [{allow: public}]) {
  id: ID!
  authid: Text
  tid: Text
  name: Text
}

enum Text {
  VALUE
}
 